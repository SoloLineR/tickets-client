export type Ticket = {
  id: number;
  title: string;
  description: string;
  amount: number;
  price: number;
  img: string;
};
export type IUserData = {
  email: string;
  roleid: number;
  userid: number;
};
